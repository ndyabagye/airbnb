'use client';

import { Toaster } from 'react-hot-toast';

// TODO: Get explanation why the toaster can be used with a provider...
const ToasterProvider = () => {
  return (
    <Toaster />
  );
}

export default ToasterProvider;
